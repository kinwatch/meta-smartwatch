# eos: modules whose .ko files live on the rootfs (/lib/modules/<uts>/vendor,
# shipped by eos-vendor-modules) and load after systemd mounts the rootfs.
# modprobe resolves dependencies from modules.dep; order is informational.
# Milestone 2a = WiFi only. Audio/BT/display/sensors autoloads come with the HALs.

# --- WLAN (qcacld-3.0 + CNSS platform glue) ---
cfg80211
cnss_prealloc
cnss_utils
cnss_nl
wlan_firmware_service
icnss2
google_wlan_mac
wlan
