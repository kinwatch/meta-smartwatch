#!/bin/sh

# CPU0 governor: schedutil ramps quickly for bursty UI workloads.
echo schedutil > /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor

# Core count belongs to kin-cpu-cores, not here. A live call has one process
# serving the microphone, the network and the speaker every 20 ms, and on a
# single core it starves its own audio.
