# aurora: Halium-13 / Android-13 (Wear OS 4). API 33 preset points /dev/binder at
# the aidl3 servicemanager + protocol so the host can reach the container's
# framework/AIDL services (the IRadio HAL). hwbinder is untouched (stays HIDL),
# so bluebinder and the HIDL HALs keep working.
[General]
ApiLevel = 33
